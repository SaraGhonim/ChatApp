<html>
  <head>
    <title>Chat App</title>
    <style>
         body { margin: 0; padding-bottom: 3rem; font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif; }
         #name {margin-left:1rem;}
         #owner {margin-left: 1rem;}
         #form{ border-radius: 1rem ;background: rgba(7, 34, 187, 0.15); padding: 0.25rem; position: fixed; bottom: 0; left: 40; right: 40; display: flex; height: 3rem;width:37rem; box-sizing: border-box; backdrop-filter: blur(10px); }
         #friend { border: none; padding: 0 1rem; flex-grow: 1; border-radius: 2rem; margin: 0.25rem; }
         #friend:focus { outline: none; }
         #friend::placeholder { color: rgb(119, 116, 116);opacity: 1; }
         #form > button { background: #03026e; border: none; padding: 0 1rem; margin: 0.25rem; border-radius: 1rem; outline: none; color: #fff; }


         #messages { list-style-type: none; margin-right:1rem;margin-left: .3rem; padding: 0;}
         #messages > li { padding: 0.5rem 1rem;margin-left:0.6rem;background:rgba(44, 69, 211, 0.15);border-radius: 2rem;  }
         #messages > h5 { padding: 0.5rem 1rem;margin-left:0.6rem;border-radius: 2rem;margin-bottom: 0.1rem;  }
  
  
  </style>

 </head>

 <body>
<h1 id="name">Welcome <%=data.name%></h1>

<h3 id="name"><%=data.m%></h3>
<h4 id="owner"> owner : <%=data.messages[0].owner%></h4>

   <ul id="messages">
       <% data.messages.forEach((item)=>{%>
        <h5> <%=item.friend%> </h5>
        <li> <%=item.lastMessage%> </li>
      <%});%>
      
    </ul>

    <form id="form" action="http://localhost:3000/Chats/getChat" method="GET">
      <input id="friend" autocomplete="off" name="owner" placeholder="enter your email... ? "/>
      <input id="friend" autocomplete="off" name="friend" placeholder="Start Chating with which friend... ? "/><button>Start</button>
    </form>


 </body>

 <script ></script>

 </html>